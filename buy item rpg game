local RS = game:GetService("ReplicatedStorage")

for Index, Variable in pairs(RS.GameContents.Items:GetChildren()) do
 game:GetService("ReplicatedStorage").GameContents.Remotes.BuyEvent:FireServer(Variable.Name)
end
